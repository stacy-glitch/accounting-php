# 2025-11-10 — 名冊資料庫化／中油 CSV

## 今日結果
- 勞保、健保、中油名冊全改為「API + DB」架構：上傳 CSV/XLSX/PDF 直接解析並寫入資料表（`labor_roster_records`、`health_roster_records`、`cpc_records`），頁面載入時呼叫 API 取得資料，編輯/刪除立即更新資料庫。
- 健保表頁面新建，UI 與勞保一致（月份導覽、靠右上傳鈕、編輯/刪除）。中油表也換成同樣的卡片版面，只顯示系統需要的欄位。
- 勞保/健保上傳支援 PDF（`pdftotext`）與 CSV/XLSX；中油僅 CSV，但允許檔案含多餘欄位，系統只抓「車牌、司機、交易日期、油站、金額、備註」並依車牌＋日期排序。

## 實作重點
- **SQL**：`sql/20251110_create_labor_roster.sql`、`..._health_roster.sql`、`..._cpc_records.sql` 建立三張資料表（務必在 DB 執行）。
- **API**：
  - `api/payroll/labor_records.php` / `labor_upload.php`
  - `api/payroll/health_records.php` / `health_upload.php`
  - `api/payroll/cpc_records.php` / `cpc_upload.php`
  上傳 API 會刪除舊資料→寫入新資料→回傳最新紀錄。
- **前端**：`assets/js/payroll-labor.js`、`payroll-health.js`、`payroll-cpc.js` 改為 fetch API；`payroll/labor.php`、`health.php`、`cpc.php` 及 `admin.css` 調整卡片與工具列樣式。

## 仍需處理 / 風險
- 本地 `pdftotext` 路徑目前寫死 `/opt/homebrew/bin/pdftotext`；若部署環境不同需調整。
- 中油 CSV 只抓「車牌」欄位；若來源檔沒有車牌資料會被視為無效列。
- 尚未做後端驗證／權限控管，API 目前預設可信來源。

## 推薦下一步
1. 在正式 DB 執行 3 份 SQL，並確認 `api/config.php` 連線設定正確。
2. 用實際勞保/健保 PDF 與中油 CSV 測試整條流程，若欄位命名與範例不同，補上對應 mapping。
3. 考慮為 API 增加匯出、查詢條件（依車牌、司機等）與權限驗證。
