# 2025-11-08 — 薪資列印與模板調整

## 今日結果
- 批次列印面板整合到薪資表卡片內：列印按鈕旁新增可多選的員工代號下拉，移除舊卡片與提示文字。
- 列印樣式縮減：`.payroll-sheet` 高 120mm、字體與 padding 精簡，A4 直式可上下列印兩位員工；若只列印一人，下半部會留白。
- 新增「薪資模板」卡片：可切換員工、預覽並編輯支出/收入項與備註，提供「新增模板」「儲存模板」按鈕，目前資料暫存在前端 `templateStore`。

## 實作重點
- **版面調整**  
  - `public_html/accounting/payroll/index.php`：刪除批次列印卡片；在薪資卡片 actions 新增多選下拉與列印按鈕，並加入模板管理卡片。  
  - `public_html/accounting/assets/css/admin.css`：新增 inline 多選樣式、模板卡片樣式；列印模式下縮小 `.payroll-sheet` 尺寸與字級。  
  - `public_html/accounting/assets/js/payroll-index.js`：以多選下拉控制批次列印；新增模板狀態、帶入/儲存邏輯；列印與畫面都改用模板資料。

- **模板檢視/儲存**  
  - 模板預設值目前使用內建 `FORMULAS`，儲存後暫存於 `templateStore`（頁面刷新即失）。  
  - 頁面可立即預覽合計與收入－支出，並可套用到當前員工。

## 仍需處理 / 風險
- 模板資料尚未落地（沒有 API / DB），刷新即失，無法多人共同維護。
- 多選列印尚缺搜尋/篩選與全選，選單僅依載入順序呈現。  
- 列印高度若再加入長備註可能超出 120mm，未加自動縮放。

## 推薦下一步
1. 設計薪資模板資料表與 API（例如 `payroll_presets` + `employee_payroll_preset`），將前端暫存改為持久化。  
2. 列印流程整合模板與備註銀行欄設定，確保批次列印與單張列印一致。  
3. 為模板卡片加入表單驗證、搜尋員工、多版本管理等需求。  
4. 增加列印多選下拉的搜尋/分類或分頁，避免員工清單過長。
