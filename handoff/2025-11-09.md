# 2025-11-09 — 薪資列印＆模板 UI 調整

## 今日結果
- 薪資列印控制（列印員工下拉＋列印按鈕）移至「本月薪資紀錄」卡片底部，讓薪資表主卡片只保留日期與員工欄位。
- 「本月薪資紀錄」卡片移到「新增模板」之前，表頭改為 "‹ 上月｜xx 年 xx 月薪資紀錄｜下月 ›" 並預設顯示前一個月份；選擇員工後可預覽完整薪資表。
- 列印排版再調整：`payroll-sheet` 移除外框與 padding，並在上下兩張表間保留 8mm 空白供對折。

## 實作重點
- `public_html/accounting/payroll/index.php`：調整卡片順序、更新列印控制位置、加入薪資紀錄預覽卡。
- `public_html/accounting/assets/css/admin.css`：新增薪資紀錄卡版面（置中標題、左右按鈕）、列印樣式去除外框、增加預覽表格樣式。
- `public_html/accounting/assets/js/payroll-index.js`：月份預設改為前一個月；儲存與預覽薪資紀錄使用新卡片結構；列印控制與員工多選維持可用。

## 仍需處理 / 風險
- 模板與薪資紀錄仍暫存在前端（`templateStore`、`savedSheets`），刷新即失；需規畫後端資料表與 API。
- 薪資紀錄僅簡易匯總，尚未與後端資料連動；目前僅供畫面預覽。
- 列印多選下拉仍缺搜尋/分類，全員列表可能過長。

## 推薦下一步
1. 資料庫化薪資模板與薪資紀錄（新增 `payroll_presets`、`payroll_records` 等表）並提供 API。
2. 在薪資紀錄卡片加上搜尋/篩選、日期快速定位，或支援匯出功能。
3. 列印多選下拉加入搜尋、部門分類或全選按鈕，改善大量員工操作體驗。
