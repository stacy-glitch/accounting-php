# JudaCargo Accounting — Handoff (v20251228)

## TL;DR（若只想快速接手，先讀這段即可）
1. 新增了基隆二信、兆豐銀行及匯款帳號管理三個頁面；可上傳資料並在前端行內編輯對帳與對應表單。
2. 應收票據/兆豐/二信的日期、備註等欄位格式統一（ROC 斜線、移除特定字樣），備註置左；匯款帳號客戶欄可搜尋資料維護的客戶。
3. PDF/JPG 仍僅保存原檔、未解析；兆豐尚未支援 `.xls`，需另存 `.xlsx`；匯款帳號資料目前只存在前端。
4. 明日優先：串接匯款帳號 CRUD API、決定兆豐/二信資料的持久化策略（DB or JSON）、評估 `.xls` 解析方案與 PDF 預覽需求。
5. 若需背景，往下依「最新交接 → 目前任務 → 參考資料 → 過往紀錄」閱讀；每節已標註必讀/選讀。

---

## 最新交接（必讀 ≈3 min）
### 今日完成
- **帳戶匯入頁面**：新增 `klsb.php`（基隆二信）與 `mega-bank.php`（兆豐銀行）以及對應 JS / API `_klsb_parser.php`, `_mega_parser.php` 等。支援 CSV/XLSX 解析、ROC 日期格式化、備註清理；僅開放對帳/對應表單編輯，備訽欄置左。
- **匯款帳號管理**：新增 `remittance.php` + `sales-remittance.js`，表單欄位（客戶、匯款備註、匯款銀行、備註）／列表操作完成，客戶欄可搜尋資料維護客戶（代號或中文）。目前資料僅存於前端 state。
- **應收票據維護**：新增 API `notes_create/delete`，前端可新增/刪除並統一 ROC 斜線日期，合計改為逐筆累積；刪除/新增導入 `record_key`，備註欄移除 PDF 殘留字樣。
- **側邊欄/樣式**：基隆二信、兆豐銀行新增 `sidebar.js`，避免零用金按鈕失效；兆豐表格備註欄改靠左。

### 待解決 / 風險
- 兆豐 `.xls` 仍無法解析；上傳會保留原檔但沒有資料列。
- 匯款帳號列表尚未串 DB/API，重新整理會清空；刪除/編輯也只在前端生效。
- 基隆/兆豐資料目前儲存在 `uploads/.../latest.json`，尚未決定是否需寫入資料庫或提供匯出。

### 明日優先
1. **匯款帳號 CRUD**：設計資料表與 API，串前端新增/編輯/刪除，並決定是否要與客戶資料關聯。
2. **帳戶匯入持久化**：討論二信/兆豐資料是否要寫入 DB 或提供下載；若仍使用 JSON，至少要有備份與匯出機制。
3. **XLS 支援/OCR**：評估導入 `.xls` 解析方案（第三方套件）與掃描檔 OCR/人工輸入流程。

---

## 目前任務與狀態
### 應收票據（選讀，需背景）
- 上傳：`api/sales/notes_upload.php` 解析 CSV/XLSX，寫入 `uploads/sales-notes/YYYYMM/latest.json`。
- 下拉：`notes_latest.php`；前端 `sales-notes.js` 負責 ROC 日期、月份顯示與新增/刪除（前端狀態）。尚未串實際 DB。

### 帳戶匯入（基隆二信／兆豐銀行）（必讀 2 min）
- 新增頁面 `klsb.php`、`mega-bank.php` + JS／API。支援年月切換、上傳 CSV/XLSX、RO C日期顯示、備註清理；操作欄僅提供編輯（對帳／對應表單）。PDF/JPG 只保存原檔，未解析。
- 限制：兆豐 `.xls` 未支援；上傳時會提示「目前僅能解析 CSV、XLSX」。

### 匯款帳號管理（必讀 1 min）
- 表單欄位：客戶、匯款備註、匯款銀行、備註；可輸入客戶代號或中文，會自動帶出名稱；新增記錄會加到下方列表，列表支援行內編輯/刪除（僅前端）。
- TODO：需要 DB schema + CRUD API，決定儲存位置（可放在 `remittance_accounts` 表）。

---

## 參考資料（選讀）
- **環境資訊**：macOS + zsh／MAMP；MySQL `127.0.0.1:8889`，DB `judacargo_local`；本地伺服器指令 `php -S 127.0.0.1:8001 -t public_html/accounting`。
- **敏感檔案**：`api/config.php`、各種匯入檔（CSV/XLSX/PDF）；嚴禁推到公開 repo。
- **歷史交接**：詳細內容都放在 `handoff/`（例：`handoff/2025-11-01.md`），若需追溯請先看該日期檔。這些檔案可視為附錄，避免 AGENTS.md 過肥。

---

## 過往紀錄（選讀）
- 2025-10-22：完成主檔匯入第一版與側邊導覽（詳見 `handoff/2025-10-22.md`）。
- 2025-10-31：恢復 ROC 客製日曆、修復代墊欄位（`handoff/2025-10-31.md`）。
- 其他歷史紀錄請參考 `handoff/` 目錄（依日期劃分）。
